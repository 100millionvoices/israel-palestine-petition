<h1 class="text-center lead"><%= t('signatures.confirmation_success') %></h1>

<div class="fluid_container text-center message-text">
  <p class="lead"><%= t('signatures.confirmation_html', home_link: link_to(t('signatures.home_page'), home_path)) %></p>

  <p class="text-left bottom-links">/ <%= link_to(t('links.home').downcase, home_path) %></p>
</div>

<%= render 'share_petition' %>
